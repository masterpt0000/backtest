# REST QuestDB: porta 9000 (não uses só :80 a menos que tenhas proxy para /exec)
QUESTDB_HTTP_URL=http://192.168.1.216:9000
QUESTDB_CANDLES_TS_COL=local_ts
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# PostgreSQL: utilizador postgres + a password que definiste na instalação (owner de backtest_app).
# Uma única linha DATABASE_URL — porta por defeito 5432.
DATABASE_URL=postgresql+psycopg://postgres:OGAITmestre0000@127.0.0.1:5432/backtest_app
